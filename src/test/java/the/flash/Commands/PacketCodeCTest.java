package the.flash.Commands;

import org.junit.Assert;
import org.junit.Test;
import the.flash.Serializer.Serializer;

public class PacketCodeCTest {
    @Test
    public void codec() {
//        Serializer serializer = Serializer.DEFAULT;
//        LoginRequestPacket packet = new LoginRequestPacket();
//        packet.setUserId("1");
//        packet.setUsername("ray");
//        packet.setPassword("success");
//        PacketCodeC coder = new PacketCodeC();
//        LoginRequestPacket newPacket =  (LoginRequestPacket)coder.decode(coder.encode(packet));
//
//        Assert.assertArrayEquals(serializer.serialize(newPacket), serializer.serialize(packet));
    }
}
